% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fglmm.R
\name{fglmm}
\alias{fglmm}
\title{Fit a fast generalised linear mixed model}
\usage{
fglmm(
  formula,
  data,
  family = binomial(),
  control = fmlm_control(),
  weights = NULL,
  na.action = na.omit,
  subset = NULL,
  contrasts = NULL,
  verbose = 0L
)
}
\arguments{
\item{formula}{A two-sided formula with random effects in bar notation.}

\item{data}{A data frame.}

\item{family}{A family object (e.g., \code{binomial()}, \code{poisson()})
or a string (\code{"binomial"}, \code{"poisson"}, \code{"Gamma"}).}

\item{control}{A list of control parameters; see \code{\link{fmlm_control}}.}

\item{weights}{Optional prior weights.}

\item{na.action}{Function for handling NAs.}

\item{subset}{Optional subset expression.}

\item{contrasts}{Contrast specifications for factors.}

\item{verbose}{Integer verbosity level.}
}
\value{
An object of class \code{\linkS4class{fglmmMod}}.
}
\description{
Fits a GLMM using Penalised Iteratively Reweighted Least Squares (PIRLS)
with a Laplace approximation, optimised in C++. Uses the same formula
syntax as \code{\link[lme4]{glmer}}.
}
\examples{
\dontrun{
# Binomial GLMM
m <- fglmm(cbind(incidence, size - incidence) ~ period + (1 | herd),
            data = lme4::cbpp, family = binomial())
summary(m)

# Poisson GLMM
m2 <- fglmm(count ~ treatment + (1 | site), data = mydata,
             family = poisson())
}

}
